# Phase 4: Build a Standard candidate

Status: implementation in progress. Not accepted, merged, or deployed.

Accepted base inspected on 2026-09-22: `1a0909e9e3417c5eaf1f48cafc52da1be9a2d4ee`, version `0.1.0-dev.3`. Git remote main, trusted staging HTTPS build metadata, and VPS immutable current release agree. Accepted main CI and deployed verification succeeded. No AGENTS.md is present in this checkout.

## Candidate contract

Build a Standard produces a user-authored Personal Standard with Status: Set. A goal names an outcome; a standard defines behavior or boundaries the user owns. The tool teaches observability, supporting structure, deliberate adaptation versus negotiation, and correction without punishment or identity judgment. No scores, recommendations, rewriting, Reset workflow, or unavailable handoff.

Reuse accepted shared controls, artifact printing, local storage and synchronization. Extend Saved Work compatibly across all three artifact types. Memory only until explicit saving. Preserve the global 50-record limit, unknown-record handling, and oe:-scoped deletion.

## Verification gates

Run all existing tests plus standard definition/list/review/artifact/storage/security/privacy/accessibility coverage across Chromium, mobile Chromium, Firefox, and WebKit with zero retries. Inspect screenshots and multi-page print output. Record actual screen-reader limits. Compare clean strict local build to independent CI artifact and trusted deployed files. Deploy immutable feature SHA, preserve accepted main rollback, verify existing backups and neighboring sites.

The draft PR stays unmerged until Jim's explicit product review. Production remains untouched. Detailed executed evidence will replace this in-progress status before candidate delivery. Historical phase acceptance records remain unchanged.
